version: '3'
services:
  sonarqube:
    build: ./config/sonarqube
    ports:
      - 9000:9000
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - 8080:8080
      - 50000:50000
    volumes:
     - ${PWD}/jenkins:/var/jenkins_home
